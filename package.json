{
  "name": "workspace",
  "version": "0.0.0",
  "homepage": "https://emmajoe44.github.io/Agri-ssd",
  "license": "MIT",
  "scripts": {
    "preinstall": "node -e \"const fs=require('fs'); for (const f of ['package-lock.json','yarn.lock']) { try { fs.rmSync(f,{force:true}); } catch {} }\"",
    "build": "npx pnpm run typecheck && npx pnpm -r --if-present run build",
    "typecheck:libs": "tsc --build",
    "typecheck": "npx pnpm run typecheck:libs && npx pnpm -r --filter \"./artifacts/**\" --filter \"./scripts\" --if-present run typecheck",
    "predeploy": "npx pnpm --filter @workspace/agri-market run build",
    "deploy": "gh-pages -d artifacts/agri-market/dist/public"
  },
  "private": true,
  "devDependencies": {
    "gh-pages": "^6.3.0",
    "prettier": "^3.8.1",
    "typescript": "~5.9.2"
  }
}